import { message, Button, Descriptions, List, Image, Tag, Spin, Table } from 'antd';
import React, { useState, useEffect } from 'react';

export const User: React.FunctionComponent = () => {

    useEffect(() => { }, []);

    return (<>User</>);
};